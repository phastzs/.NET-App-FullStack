@model IEnumerable<Project_63131016.Models.LoaiSach>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiSach)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenLoaiSach)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiSach }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiSach }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiSach })
        </td>
    </tr>
}

</table>
